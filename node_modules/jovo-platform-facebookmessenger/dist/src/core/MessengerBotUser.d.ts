import { User } from 'jovo-core';
import { UserProfile } from '../Interfaces';
import { MessengerBot } from './MessengerBot';
export declare class MessengerBotUser extends User {
    private readonly messengerBot;
    profile?: UserProfile;
    constructor(messengerBot: MessengerBot);
    getId(): string | undefined;
    fetchProfile(fields?: string): Promise<UserProfile | undefined>;
    fetchAndSetProfile(fields?: string): Promise<void>;
}
