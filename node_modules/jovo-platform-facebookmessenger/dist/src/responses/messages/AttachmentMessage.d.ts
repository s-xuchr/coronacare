import { ApiVersion, AttachmentType, IdentityData, Message, QuickReply } from '../..';
export interface AttachmentMessageFile {
    path: string;
    fileName: string;
    mimeType?: string;
}
export interface AttachmentMessageOptions {
    type: AttachmentType;
    data: AttachmentMessageFile | string | number;
    isReusable?: boolean;
    quickReplies?: Array<QuickReply | string>;
}
export declare class AttachmentMessage extends Message {
    readonly recipient: IdentityData;
    private readonly options;
    constructor(recipient: IdentityData, options: AttachmentMessageOptions);
    send(pageAccessToken: string, version: ApiVersion): Promise<any>;
    private sendFile;
    private isUrlData;
    private isAttachmentIdData;
    private isFileData;
}
