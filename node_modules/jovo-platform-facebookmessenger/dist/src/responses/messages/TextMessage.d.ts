import { IdentityData, Message, MessageType, QuickReply } from '../..';
export interface TextMessageOptions {
    text: string;
    messageType?: MessageType;
    quickReplies?: Array<QuickReply | string>;
}
export declare class TextMessage extends Message {
    readonly recipient: IdentityData;
    message_type: MessageType;
    message: {
        text: string;
        quick_replies?: QuickReply[];
    };
    constructor(recipient: IdentityData, options: TextMessageOptions);
}
