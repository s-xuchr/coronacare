import { IdentityData, Message, SenderActionType } from '..';
export declare class SenderAction extends Message {
    readonly recipient: IdentityData;
    sender_action: SenderActionType;
    constructor(recipient: IdentityData, sender_action: SenderActionType);
}
