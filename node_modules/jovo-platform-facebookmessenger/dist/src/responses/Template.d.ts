import { IdentityData, Message, TemplateType } from '..';
export interface TemplatePayload<T> {
    template_type: T;
}
export declare abstract class Template<P extends TemplatePayload<TemplateType>> extends Message {
    readonly recipient: IdentityData;
    message: {
        attachment: {
            type: 'template';
            payload: P;
        };
    };
    constructor(recipient: IdentityData, payload: P);
}
