import { TemplateType } from '../..';
import { Button } from '../Button';
import { Template, TemplatePayload } from '../Template';
export declare type ButtonTemplateOptions = Omit<ButtonTemplatePayload, 'template_type'>;
export interface ButtonTemplatePayload extends TemplatePayload<TemplateType.Button> {
    text: string;
    buttons: Button[];
}
export declare class ButtonTemplate extends Template<ButtonTemplatePayload> {
}
