import { Button, WebViewHeightRatio } from '../..';
export interface LinkButtonOptions {
    webview_height_ratio?: WebViewHeightRatio;
    messenger_extensions?: boolean;
    fallback_url?: string;
    webview_share_button?: string;
}
export declare class LinkButton extends Button {
    title: string;
    url: string;
    constructor(title: string, url: string, options?: LinkButtonOptions);
}
