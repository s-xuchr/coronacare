import { Button } from '../..';
export declare type GameMetaData = {
    player_id: string;
} | {
    context_id: string;
};
export declare class GameButton extends Button {
    title: string;
    payload: string;
    game_metadata: GameMetaData;
    constructor(title: string, payload: string, game_metadata: GameMetaData);
}
