import { HandleRequest, Plugin } from 'jovo-core';
import { FacebookMessenger, MessengerBot } from '..';
export declare class FacebookMessengerCore implements Plugin {
    private launchPayload?;
    private locale?;
    install(messenger: FacebookMessenger): void;
    init(handleRequest: HandleRequest): Promise<void>;
    request(messengerBot: MessengerBot): Promise<void>;
    type(messengerBot: MessengerBot): Promise<void>;
    session(messengerBot: MessengerBot): Promise<void>;
    output(messengerBot: MessengerBot): Promise<void>;
}
