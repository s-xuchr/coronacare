import { BaseApp, ExtensibleConfig, HandleRequest, Platform, TestSuite } from 'jovo-core';
import { ApiVersion, FacebookMessengerRequestBuilder, FacebookMessengerResponseBuilder, MessengerBotRequest, MessengerBotResponse } from '.';
export interface UpdateConfig<T> {
    updateOnSetup?: boolean;
    data?: T;
}
export interface GreetingElement {
    locale: string;
    text: string;
}
export interface Config extends ExtensibleConfig {
    shouldOverrideAppHandle?: boolean;
    greeting?: UpdateConfig<GreetingElement[]>;
    launch?: UpdateConfig<string>;
    pageAccessToken?: string;
    verifyToken?: string;
    locale?: string;
    version?: ApiVersion;
    userProfileFields?: string;
    fetchUserProfile?: boolean;
}
export declare class FacebookMessenger extends Platform<MessengerBotRequest, MessengerBotResponse> {
    requestBuilder: FacebookMessengerRequestBuilder;
    responseBuilder: FacebookMessengerResponseBuilder;
    config: Config;
    constructor(config?: Config);
    getAppType(): string;
    install(app: BaseApp): void;
    setup(handleRequest: HandleRequest): Promise<void>;
    request(handleRequest: HandleRequest): Promise<void>;
    initialize(handleRequest: HandleRequest): Promise<void>;
    nlu(handleRequest: HandleRequest): Promise<void>;
    afterUserLoad(handleRequest: HandleRequest): Promise<void>;
    beforeHandler(handleRequest: HandleRequest): Promise<void>;
    output(handleRequest: HandleRequest): Promise<void>;
    response(handleRequest: HandleRequest): Promise<void>;
    makeTestSuite(): TestSuite<FacebookMessengerRequestBuilder, FacebookMessengerResponseBuilder>;
    private isVerifyRequest;
    private overrideAppHandle;
}
